version: '3.5'
services:
  az-function-test:  # Change this to a unique name for your service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9090:80"
    volumes:
      - ./config.json:/home/site/wwwroot/config.json